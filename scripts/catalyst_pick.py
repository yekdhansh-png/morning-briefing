#!/usr/bin/env python3
"""
Step 2: 从合并的多路信源里选 TOP 2 利好催化事件

输入：
  candidates: list[dict] — 由 generate_briefing 准备好的候选池，每条至少含
              {source, section, title, body, weight}
  exclude_titles: list[str] — 要闻 TOP 3 的标题（去重排除）

输出：list[dict]，2 条 TOP 利好：
  {
    "rank": 1,
    "source": "上证早知道·上证聚焦",
    "title": "绿电直连政策升级 绿电价值迎来重估",
    "concept": "绿电",           # 概念名（≤6 字）
    "catalyst": "...",           # 催化逻辑（30-50 字）
    "reason": "..."              # 为什么今天可能涨（40-60 字，含'有望/或'等节奏副词）
  }

选股逻辑（三层硬约束）：
  A. 热门度优先：优先选当前市场主线赛道
  B. 涨幅潜力：今天最可能开盘高开/盘中拉升的
  C. 强制去重：不能跟要闻 TOP 3 重复（按 title 关键词模糊判定）
"""
from __future__ import annotations

import json
import sys

from llm_client import chat_json, LLMError


# 当前 A 股主线赛道白名单（2026 年）——LLM 在分数接近时优先这些方向
HOT_CONCEPTS_HINT = """
当前 A 股主流热门赛道（按市场关注度排序）：
- AI 算力 / 服务器 / 液冷 / 光模块 / 光通信 / 交换机
- 半导体 / 存储芯片 / HBM / 国产芯片 / 先进封装 / 半导体设备
- 智能驾驶 / 智能眼镜 / AI 终端 / 机器人 / 具身智能
- 商业航天 / 卫星互联网 / 低空经济 / eVTOL
- 新能源车 / 锂电池 / 固态电池 / 充电桩
- 绿电 / 风电 / 光伏 / 储能 / 核电
- 创新药 / CXO / 减肥药 / 医疗器械
- 军工 / 大飞机 / 国防装备
- 有色金属（黄金、铜、稀土、锂、镍）/ 油气 / 化工
- 出口链 / 跨境电商 / 一带一路
- 数字货币 / 跨境支付 / 稳定币
- 农业 / 转基因 / 生猪养殖
- 国资改革 / 央企重组 / 并购重组

冷门 / 降级处理：
- 单一地区性基建 / 小县城项目
- 远期 5 年规划没具体落地的
- 政策"征求意见稿"无具体补贴金额/时间表
- 已发酵超过 3 天的事件
- 单个小市值公司（< 100 亿）的内部变动
"""


SYSTEM_PROMPT = f"""# 角色
你是一位 A 股短线题材交易员，每天 7:30 看完早报选出"今日最可能涨的 2 个利好催化"。
你的判断标准只有一个：**今天开盘哪个概念最容易高开高走、最容易吸引资金。**

# 任务
我会给你今日抓到的"利好备选事件池"（来自上证早知道、财联社早报、证券时报等多路），
请你按下方"三层选股逻辑"选出 TOP 2，并给出概念+催化+涨幅理由。

# 三层选股逻辑（必须按顺序执行）

## 第 1 层：硬过滤——去重
已被早报"重磅要闻"模块占用的事件**绝对不能再选**。我会在 user prompt 里给你
"requires_exclude" 列表，那些标题对应的事件、以及它们所属的同一概念主题，
都要从候选中剔除。
- 判断标准不是严格字符串匹配，而是**主题级别**——只要催化逻辑相同就视为重复
  例：要闻已有"英伟达财报超预期" → 即便备选池里有"AMD Q1 营收超预期"，
  二者催化逻辑接近（都是 AI 算力业绩兑现），**禁止再选 AI 算力**
- 如果剔除后剩余候选 < 2，就在保证不雷同的前提下选差异最大的

## 第 2 层：热门度筛选——优先主线赛道
{HOT_CONCEPTS_HINT}

打分规则：
- 主线赛道（白名单内）+ 强催化 → ★★★ 优先入选
- 主线赛道 + 中等催化 → ★★ 候选
- 非主线但具体可定价（公司大单、政策落地）→ ★ 候选
- 冷门/远期/小众 → ✗ 直接淘汰

## 第 3 层：涨幅潜力——边际定价（与要闻 prompt 同源）
判断"今天开盘涨不涨"的核心是**未计价的边际增量**：

时效优先级：
- ★★★ 凌晨/昨日盘后（15:00 后）发布 → 今日盘前未计价，开盘最易高开
- ★★ 昨日盘中（09:30-15:00） + 当日尾盘还没炒到位 → 仍有续作空间
- ✗ 24 小时前的旧闻 → 已计价，不选

强度优先级：
- 政策细则落地（含具体补贴金额/受益清单/时间表） > 政策意见稿
- 龙头公司大单/超预期业绩 > 一般业绩预告
- 海外产业链异动直接映射 A 股 > 远期前瞻
- 含机构观点（券商研报、大行预测）背书 > 纯事实
- 量化数据支撑（如"出货量翻倍/订单 X 亿"）> 模糊定性

## 第 4 层：事件确定性 / 信源等级（CRITICAL 风控维度）

利好的"含金量"由**事件来源的确定性**决定。确定性越高，板块联动概率越大，
今日开盘越容易兑现为股价上涨。按以下五级金字塔评估：

  **S 级（最高）：官方文件强制力**
  - 部委正式发文 / 国务院常务会决定 / 央行/证监会政策落地
  - 央地双方联合发文（如"发改委+能源局"）
  - 行业部委印发"实施方案/管理办法/通知"（含具体补贴金额或时间表）
  - 国资委、上交所、深交所等官方监管动作
  例：发改委+能源局发布《多用户绿电直连发展通知》 → S 级（绿电）

  **A 级：龙头公司公告 / 业绩兑现**
  - 沪深主板/科创板龙头公司**正式公告**（盖章）：大单中标、定增、并购、业绩快报
  - 涉及金额量化清晰（X 亿元、X% 增速、X 个百分点）
  - 港股龙头当日早盘已有反应（A 股大概率跟随）
  - 行业 ETF 重仓股的研报上调评级
  例：璞泰来公告拟 56 亿投建锂电池隔膜项目 → A 级（锂电材料）

  **B 级：海外产业链确认**
  - 海外巨头业绩/发布会**当晚已披露的数据**（英伟达/谷歌/Apple/特斯拉等）
  - 国际权威媒体（路透/彭博）首发的具体数据型新闻
  - 国际行业组织正式数据（IEA/OPEC/SEMI 等）
  例：谷歌发布会确认 Gemini 智能眼镜 2026 秋上市 → B 级（智能眼镜）

  **C 级：行业协会 / 头部机构观点**
  - 中汽协/中钢协/中国基金业协会等行业数据
  - 中金/华泰/中信等头部券商**重磅观点**（如"维持/上调评级"）
  - 大摩/高盛等外资行业预测（含量化）
  例：大摩预测国内 AI 芯片自给率 2030 年达 86% → C 级（国产芯片）

  **D 级：媒体传闻 / 小作文（基本不选）**
  - 含"据传/接近 X 的人士透露/有市场消息称"等弱化表述
  - 来源仅为自媒体 / X 推特 / 雪球贴
  - 无具体数字、无文件、无公司公告佐证
  例："听说某龙头拿到大单" → D 级（直接淘汰）

**应用规则**：
1. S 级 + A 级 = **首选**（确定性 + 板块联动概率最高）
2. B 级 = **候选**（如果时效新、含具体数据）
3. C 级 = **辅助**（用于补强已有 S/A 级催化的逻辑链）
4. D 级 = **基本不选**（除非该传闻已被多家主流媒体复用，转 C 级处理）

5. 两条 TOP 必须**至少有一条是 S 级或 A 级**——避免两条都是机构观点这种弱催化
6. 在输出 JSON 中加 `event_level` 字段（S/A/B/C/D），并在 `catalyst` 字段中
   **明确点出信源**（如"国家发改委发文"/"龙头公司公告"/"谷歌发布会确认"）

## 第 5 层：行业基本面信号（CRITICAL 加分项）

事件级别只看"信源"，但**真正驱动股价的是产业基本面的边际变化**。
当一条新闻反映出**量、价、供需、周期**层面的实质改善时，应当显著加权。

### 五大基本面信号（识别 + 加分）

1. **价格信号 ★★★**：行业产品价格变动
   - 工业品：DRAM/光模块/钴/锂/锗/稀土/钨/银/海运 等价格涨跌
   - 消费品：猪肉/电力/天然气/油气 等价格信号
   - 关键词："涨价 X%"/"创 X 年新高/新低"/"价格止跌反弹"/"价差扩大"

2. **供需信号 ★★★**：产业链供需矛盾
   - 供给：扩产/减产/停产/检修/出口管制/库存周期
   - 需求：订单激增/订单可见 X 个月/客户提货积极/抢购
   - 关键词："供不应求"/"在手订单"/"开工率"/"产能利用率"/"满产"/"交付期延长"

3. **业绩 / 出货量信号 ★★**：龙头先行指标
   - 龙头公司业绩超预期/上调指引
   - 行业出货量/装机量/销量数据（同比+ X%）
   - 关键词："销量同比 +X%"/"出货翻倍"/"产能爬坡"/"营收上修"

4. **景气拐点 ★★**：周期反转信号
   - PMI/CPI/PPI 等宏观指标连续 X 月扩张/收缩
   - 行业景气指数（如猪粮比/航运指数）破阈值
   - 龙头股价创新高 + 北向加仓

5. **下游需求拉动 ★★**：终端爆款带动产业链
   - 例：DeepSeek 引爆 → 国产算力链需求
   - 例：智能眼镜爆款 → 光波导/微显示
   - 例：人形机器人出货 → 减速器/丝杠/电机

### 加分逻辑

- 含价格/供需/出货等量化数据的事件 → **直接进入 TOP 2 候选**
- 即便 event_level 是 B/C，但有强基本面信号 → 视同 A 级处理
- 既无信源（D 级）又无基本面信号 → 直接淘汰

### catalyst 字段必须显式点出基本面要素

如果选中的事件含基本面信号，`catalyst` 必须包含相关关键词，例：
- "DRAM 内存价格连涨 5 个月，存储龙头 Q1 业绩超预期"（价格+业绩）
- "光模块行业在手订单可见 Q3 满产"（供需+订单）
- "猪粮比突破红线，行业去产能进入末期"（周期+拐点）

# 输出严格 JSON Schema（字段缺一不可）
{{
  "top2": [
    {{
      "rank": 1,
      "source_section": "事件来自的章节，如'上证早知道·上证聚焦' / '财联社早报·投资机会参考'",
      "source_title": "原事件标题（保留原文，便于追溯）",
      "concept": "对应的概念/板块名，≤6 字（如'绿电'/'光通信'/'机器人'）",
      "tag": "4 字标签，**必须从下列 6 个里选 1 个**：政策聚焦 / 机构热评 / 市场热议 / 资产映射 / 涨价信号 / 景气赛道",
      "event": "1 句话提炼事件本身，**严格 ≤18 字**。要把'谁+做了什么'说清楚，能砍的修饰词全部砍掉。例：'发改委升级绿电直连政策'（13 字）",
      "catalystPath": "1 句话催化路径，≤25 字，用箭头串联'事件→机制→受益'。例：'政策落地→绿电消纳价值重估→运营商估值修复'",
      "event_level": "S / A / B / C / D 五选一（信源确定性等级，详见第 4 层规则）",
      "fundamental_signal": "事件涉及的基本面信号类型，多选用 / 分隔。可选：价格上涨 / 供需缺口 / 业绩超预期 / 景气拐点 / 需求拉动 / 产能瓶颈 / 出货增长 / 无基本面信号",
      "catalyst": "完整催化逻辑 **严格 18-25 字**（与 event 拼起来不超过 45 字，前端只展示 2 行）。必须**明确点出信源**（'发改委发文'/'公司公告'/'谷歌发布会'）+ 利好原因。例：'消纳价格中枢上移，板块价值或迎重估'（17 字）",
      "reason": "为什么今天可能涨 40-60 字。必须用'有望/或/大概率/边际'等节奏副词留余地。例：'政策落地驱动板块情绪修复，叠加电力市场化改革大主线，绿电运营商估值或迎修复，新能源 ETF 大概率高开'"
    }},
    {{
      "rank": 2,
      "source_section": "...",
      "source_title": "...",
      "concept": "...",
      "tag": "...",
      "event": "...",
      "catalystPath": "...",
      "event_level": "...",
      "fundamental_signal": "...",
      "catalyst": "...",
      "reason": "..."
    }}
  ]
}}

# 四字标签库（封闭 6 选 1，严禁自创任何新标签）

tag 字段**必须严格从下列 6 个标签中选 1 个**，绝不允许自创、改字、拆字、组合：

1. **政策聚焦** — 国家/部委/地方政府发文、政策落地、试点扩容、监管动作、顶层会议表态
   适用：S 级官方文件、政策落地
   例：'发改委+能源局发布绿电直连通知' → 政策聚焦

2. **机构热评** — 券商研报、大行预测、机构调研、评级上调、机构观点集中表态
   适用：C 级机构观点
   例：'中金预测国产 AI 芯片自给率 2030 年达 86%' → 机构热评

3. **市场热议** — 雪球/论坛/媒体广泛关注、龙虎榜、资金抢筹、北向加仓
   适用：弱催化兜底、资金面热点
   例：'某 ETF 单日净申购 X 亿，资金抢筹' → 市场热议

4. **资产映射** — **仅限大宗商品资产价格映射** A 股板块（黄金/白银/铜/原油/天然气/铁矿/煤炭/铝/锂/镍/钴/稀土等 LME/COMEX/NYMEX 价格）
   适用：大宗商品价格变动直接映射 A 股有色 / 能源 / 化工板块
   ⚠️ **严格限制**：仅大宗资产价格映射才算"资产映射"
     - ✅ 例："LME 铜价创新高 9485 美元" → 资产映射（铜价直接映射有色板块）
     - ✅ 例："COMEX 黄金突破 2700 美元" → 资产映射（金价映射黄金股）
     - ✅ 例："WTI 原油跌破 70 美元" → 资产映射（油价映射石化板块）
     - ❌ 例："谷歌发布智能眼镜" → 应是 **景气赛道**（产业事件），不是资产映射
     - ❌ 例："英伟达财报超预期" → 应是 **景气赛道**（产业景气），不是资产映射
     - ❌ 例："苹果新品发布" → 应是 **景气赛道**，不是资产映射
     - ❌ 例："美股科技股大涨" → 应是 **市场热议** 或 **景气赛道**

   **判断口诀**：必须是"大宗商品价格本身"作为催化源，海外公司事件/产品发布/股价异动一律不算资产映射。

5. **涨价信号** — 工业品/消费品价格上涨、供需缺口、紧平衡、库存告急、出口管制
   适用：基本面价格 + 供给端信号（**A 股相关产业链产品价格**，区别于资产映射的大宗商品价格）
   例：'DRAM 价格连涨 5 个月' → 涨价信号
   例：'稀土出口管制' → 涨价信号
   例：'光伏组件价格止跌反弹' → 涨价信号

6. **景气赛道** — 订单饱满、出货量激增、业绩兑现、周期反转、终端需求爆发、产业链景气，
   **海外巨头产品发布/财报兑现/新品上市** 等海外产业事件也归入此类
   适用：基本面需求 + 业绩端信号 + 海外产业链事件
   例：'光模块在手订单可见 Q3 满产' → 景气赛道
   例：'DeepSeek 引爆国产算力需求' → 景气赛道
   例：'谷歌确认 Gemini 智能眼镜 2026 秋上市' → 景气赛道（海外巨头新品 = 产业景气催化）
   例：'英伟达 Q1 财报营收 +85%' → 景气赛道（海外巨头业绩 = 产业景气催化）

## 标签选择优先级

按事件最核心特征选 1 个（不要同时贴 2 个，选最贴合的那个）：
- 事件本质是"文件 / 监管 / 政府表态" → **政策聚焦**
- 事件本质是"机构 / 券商 / 大行说什么" → **机构热评**
- 事件本质是"资金 / 论坛 / 龙虎榜" → **市场热议**
- 事件本质是"大宗商品价格变动（金/银/铜/油/气/煤/铁矿/稀土等）" → **资产映射**
- 事件本质是"海外公司财报/新品/发布会" → **景气赛道**（不是资产映射！）
- 事件本质是"价格涨 / 供给紧" → **涨价信号**
- 事件本质是"需求好 / 订单多 / 业绩出" → **景气赛道**

如果同时含多种特征，按"事件主导因素"判断：
- 既是政策又含基本面 → 优先 **政策聚焦**（信源等级更高）
- 既是机构观点又含基本面 → 优先 **景气赛道** 或 **涨价信号**（基本面更有锚定力）

# 严格约束
- 必须 2 条，**且必须分属不同主线赛道**（不能两条都是 AI 算力 / 都是新能源）
- **2 条 TOP 必须至少有一条是 S 级或 A 级**（确定性强催化），避免两条都是 B/C 弱催化
- 输出只能是 JSON，不要任何前后缀
- source_title 必须从候选池里 verbatim 复制，便于回溯
- event_level 必须如实评估，不能为了凑数把 C/D 抬到 A
- **event 字段必须 ≤18 字**，1 句话陈述事件本身（谁+做了什么），能砍的修饰词全部砍掉
- **catalyst 字段必须 18-25 字**（前端展示要求 event + catalyst 合并不超过 2 行，约 45 字总长）
- **catalystPath 字段必须 ≤25 字**，用箭头(→/⇒) 串联'事件→机制→受益'三段
- **tag 字段必须从 6 个白名单中选 1 个**（政策聚焦/机构热评/市场热议/资产映射/涨价信号/景气赛道），严禁自创、改字、组合
- concept 字段必须 ≤6 字（前端 chip 展示空间有限）
- catalyst 严禁罗列个股（"XX 涨 10%"）—— 那是要闻干的活，这里只讲事件
- catalyst 必须含**信源关键词**（"发文"/"公告"/"披露"/"发布会"/"研报"）
- reason 严禁绝对化措辞（"必涨/抄底/建议买入"）
- 如果候选池没有合适的（如全是 D 级冷门/已被要闻占用），输出 top2: [] 空数组"""


def pick_top2_catalysts(
    candidates: list[dict],
    exclude_titles: list[str] | None = None,
) -> list[dict]:
    """
    候选池 → LLM → TOP 2 利好催化

    candidates 每条至少包含：
      - source: "上证早知道·上证聚焦" / "财联社早报" / ...
      - title: 事件标题
      - body: 事件正文（含背景 + 机构观点）
      - section: 章节（用于权重）
      - weight: 0~1 浮点

    exclude_titles: 已选要闻 TOP 3 的标题列表（去重排除）
    """
    if not candidates:
        return []

    exclude_titles = exclude_titles or []

    # 拼装候选清单（截断长度避免 token 浪费）
    lines = []
    for i, c in enumerate(candidates):
        title = (c.get("title") or "")[:100]
        body = (c.get("body") or c.get("content") or "")[:280]
        source = c.get("source") or "?"
        weight = c.get("weight", 0.5)
        lines.append(
            f"[{i}] [w={weight:.1f}] <{source}> {title}\n     {body}"
        )
    listing = "\n\n".join(lines)

    exclude_block = (
        "## 已被要闻占用（禁选）\n"
        + "\n".join(f"- {t}" for t in exclude_titles)
        if exclude_titles else "## 已被要闻占用（禁选）\n（无）"
    )

    user_prompt = f"""{exclude_block}

## 候选事件池（共 {len(candidates)} 条）
{listing}

按 schema 输出 JSON。务必：
1. 严格剔除与要闻同主题的事件
2. 优先主线赛道（参考 system 的热门赛道清单）
3. 两条事件分属不同赛道
4. 优先选凌晨/昨日盘后发生、含机构观点的事件"""

    try:
        result = chat_json(
            system_prompt=SYSTEM_PROMPT,
            user_prompt=user_prompt,
            temperature=0.3,
            max_tokens=2000,
        )
    except LLMError as e:
        print(f"[catalyst_pick] LLM 失败: {e}", file=sys.stderr)
        return []

    top2 = result.get("top2") or []
    # tag 白名单兜底
    top2 = [_normalize_tag(c) for c in top2[:2]]
    return top2


# 6 个允许的标签
ALLOWED_TAGS = {
    "政策聚焦", "机构热评", "市场热议",
    "资产映射", "涨价信号", "景气赛道",
}

# 自创标签 → 白名单的映射规则（按关键词，顺序敏感，越靠前越优先）
TAG_FALLBACK_RULES = [
    # 政策聚焦（最高优先级，文件类硬信号 + 政府层面合作）
    (("发文", "政策", "部委", "国务院", "试点", "监管", "文件", "通知", "条例", "管理办法",
      "发改委", "能源局", "工信部", "证监会", "央行", "财政部",
      "国家间", "战略合作", "签署", "会谈", "条约", "首脑"), "政策聚焦"),
    # 资产映射（严格限制：仅大宗商品价格 / LME/COMEX/NYMEX/伦敦/纽约期货 / 金银铜油气煤铁矿稀土的"价格"变动）
    (("LME", "COMEX", "NYMEX", "金价", "银价", "铜价", "油价", "煤价", "铁矿石",
      "WTI", "布伦特", "现货黄金", "现货白银", "焦煤", "天然气价"), "资产映射"),
    # 涨价信号（A 股产业链产品价格 + 供给端）
    (("涨价", "提价", "缺口", "紧平衡", "库存告急", "供不应求", "出口管制", "战略储备",
      "DRAM 价格", "组件价格", "硅料价格", "锂价", "稀土价"), "涨价信号"),
    # 景气赛道（订单/业绩/需求 + 海外公司产业事件）
    (("订单", "出货", "业绩", "预增", "超预期", "满产", "景气", "周期", "需求", "爆发", "兑现",
      "财报", "发布会", "新品", "巨头", "英伟达", "谷歌", "苹果", "特斯拉", "Meta", "微软"), "景气赛道"),
    # 机构热评
    (("机构", "券商", "研报", "评级", "上调", "中金", "大摩", "高盛", "调研", "华泰", "中信"), "机构热评"),
    # 市场热议（兜底中的兜底）
    (("热议", "资金", "抢筹", "北向", "龙虎榜", "ETF", "净申购", "异动"), "市场热议"),
]


def _normalize_tag(catalyst: dict) -> dict:
    """tag 必须在白名单内，否则按关键词推断"""
    tag = (catalyst.get("tag") or "").strip()
    if tag in ALLOWED_TAGS:
        return catalyst

    # 按 catalyst / event 全文做关键词命中
    haystack = " ".join([
        catalyst.get("tag", ""),
        catalyst.get("event", ""),
        catalyst.get("catalyst", ""),
        catalyst.get("source_section", ""),
    ])
    for kws, target in TAG_FALLBACK_RULES:
        if any(k in haystack for k in kws):
            print(
                f"[catalyst_pick] tag '{tag}' 不在白名单 → fallback 到 '{target}'",
                file=sys.stderr,
            )
            catalyst["tag"] = target
            return catalyst

    # 完全无法判断时兜底为"市场热议"
    print(
        f"[catalyst_pick] tag '{tag}' 不在白名单且无关键词命中 → fallback 到 '市场热议'",
        file=sys.stderr,
    )
    catalyst["tag"] = "市场热议"
    return catalyst


if __name__ == "__main__":
    # 调试入口：用今日 SNP 上证早知道 + 财联社早报存档
    import datetime
    from pathlib import Path

    sys.path.insert(0, "scripts")
    try:
        from news_snp import fetch_szzd_today
    except ImportError:
        from scripts.news_snp import fetch_szzd_today  # type: ignore

    # 1) SNP 上证早知道
    candidates: list[dict] = []
    szzd = fetch_szzd_today()
    if szzd:
        for ev in szzd.get("events", []):
            candidates.append({
                "source": f"上证早知道·{ev['section']}",
                "section": ev["section"],
                "title": ev["title"],
                "body": ev["body"],
                "weight": ev["weight"],
            })

    # 2) 财联社早报存档
    today = datetime.datetime.now().strftime("%Y-%m-%d")
    archive = Path("data/news-archive") / f"{today}.json"
    if archive.exists():
        with archive.open() as f:
            news_data = json.load(f)
        for it in news_data.get("items", []):
            candidates.append({
                "source": f"{it.get('source','?')}·{it.get('section','?')}",
                "section": it.get("section", "?"),
                "title": it.get("title", ""),
                "body": it.get("content", ""),
                "weight": 0.5,
            })

    print(f"候选池: {len(candidates)} 条", file=sys.stderr)

    # 3) 调用 — 用 briefing.json 里的现有要闻 TOP 3 作 exclude
    briefing = json.load(open("public/briefing.json", encoding="utf-8"))
    exclude_titles = [n.get("title", "") for n in briefing.get("news", [])]
    print(f"\n要闻 TOP 3 (排除清单):", file=sys.stderr)
    for t in exclude_titles:
        print(f"  - {t[:60]}", file=sys.stderr)

    top2 = pick_top2_catalysts(candidates, exclude_titles=exclude_titles)
    print(f"\n=== TOP 2 利好催化 ({len(top2)} 条) ===")
    print(json.dumps(top2, ensure_ascii=False, indent=2))
